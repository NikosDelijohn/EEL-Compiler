program example3
	declare a,b,c,d,e,x,y,px,py,temp enddeclare
	repeat
		if not [a<c and b<d] then
			exit
		endif;
		if a=e then
			c:=c+e;
		else
			repeat
				if not [a<=d] then
					exit
				endif;
				a:=a+b;
				endrepeat;
		endif;
        i:=5;
	endrepeat;
	temp:=px;
	x:=1;
	y:=2;
endprogram

